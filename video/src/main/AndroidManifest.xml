<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhaoshuang.weixinrecordeddemo">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application>

        <activity android:name=".VideoMainActivity">
        </activity>

        <activity android:name=".EditVideoActivity"
            android:windowSoftInputMode="adjustResize|adjustPan" />

        <activity android:name=".VideoPlayActivity" />

        <activity android:name=".CutSizeActivity" />

        <activity android:name=".CutTimeActivity" />

    </application>

</manifest>